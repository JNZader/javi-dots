kind: bootstrap-module
contract_version: 0.1.0
status: extracted-slice
module:
  id: shell.fish
  owner_repo: javi-dots
  category: shell
  purpose: canonical slot for the fish shell bootstrap module
asset_roots:
  - modules/shell/fish/config/
extracted_assets:
  - modules/shell/fish/config/config.fish
  - modules/shell/fish/config/conf.d/nvm.fish
  - modules/shell/fish/config/fish_plugins
lineage:
  preferred_base: Gentleman.Dots
  legacy_delta:
    - vault/Javi.Dots/GentlemanFish/
coexistence:
  canonical_owner: javi-dots
  legacy_source: vault/Javi.Dots/GentlemanFish/
  retirement_condition: replace remaining legacy fish-only files as later bootstrap slices land
selection_rules:
  - keep shell ownership inside javi-dots
  - do not embed AI provider-specific behavior in this module
  - consume AI capabilities only through the ai consumer module when needed
